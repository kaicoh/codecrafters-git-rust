use codecrafters_git as codegit;
use codegit::Command;
use std::env;

fn main() {
    let args: Vec<String> = env::args().collect();

    if let Err(err) = Command::new(args).and_then(Command::run) {
        eprintln!("{err}");
        std::process::exit(1);
    }
}
